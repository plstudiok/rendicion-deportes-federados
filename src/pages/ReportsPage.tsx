import { BarChart3, Download, FileDown } from 'lucide-react';
import { Button, Card, Container, EmptyState, PageHeader, Skeleton, StatCard } from '@/components/ui';
import { useReportStats } from '@/hooks/useReports';
import { useToast } from '@/components/ToastProvider';
import { formatCurrency, formatError } from '@/utils/format';
export function ReportsPage() { const { data, isLoading, isError, error } = useReportStats(); const { showToast } = useToast(); if (isError) return <Container><PageHeader title="Reportes" description="Indicadores generales de la administración deportiva." /><EmptyState title="No se pudieron cargar los reportes" description={formatError(error)} /></Container>; return <Container><PageHeader eyebrow="Análisis" title="Reportes" description="Indicadores generales de la administración deportiva." action={<div className="flex gap-2"><Button variant="secondary" onClick={() => showToast('La exportación a Excel quedará disponible en el Sprint 5.')}><Download className="size-4" />Exportar Excel</Button><Button onClick={() => showToast('La exportación a PDF quedará disponible en el Sprint 5.')}><FileDown className="size-4" />Exportar PDF</Button></div>} />{isLoading ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><Skeleton className="h-32" /><Skeleton className="h-32" /><Skeleton className="h-32" /><Skeleton className="h-32" /><Skeleton className="h-32" /><Skeleton className="h-32" /></div> : <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><StatCard label="Balance General" value={formatCurrency(data?.balance ?? 0)} change="Datos reales" /><StatCard label="Ingresos Totales" value={formatCurrency(data?.income ?? 0)} change="Datos reales" /><StatCard label="Gastos Totales" value={formatCurrency(data?.expense ?? 0)} change="Datos reales" positive={false} /><StatCard label="Eventos Rendidos" value={String(data?.renderedEvents ?? 0)} change="Estado Rendido" /><StatCard label="Eventos Pendientes" value={String(data?.pendingEvents ?? 0)} change="Requieren seguimiento" /><Card className="flex items-center gap-4 p-5"><div className="rounded-xl bg-brand-50 p-3 text-brand-600"><BarChart3 className="size-6" /></div><div><p className="text-sm font-medium text-slate-500">Movimientos Totales</p><p className="mt-1 display-font text-2xl font-extrabold text-ink">{data?.movements ?? 0}</p></div></Card></div>}</Container>; }
